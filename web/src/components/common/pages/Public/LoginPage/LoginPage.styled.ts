import styled from "styled-components";

const CardOuterWrapper = styled.div`
  width: 35rem;
  margin: auto;
`;

const CardInnerWrapper = styled.div`
  display: grid;
  gap: 1.5rem;
`;

export default { CardOuterWrapper, CardInnerWrapper };
